node_modules/
dist/

# Kuzu graph databases & state
*.db
*.db.wal
*.db.lock
.kuzu/

# Build / runtime artifacts
artifacts/
coverage/

# Editor / OS
.DS_Store
*.log

# MCP server local graph DB
.engram/
.codesage/

# UDS reconcile backups — regenerable, never version-controlled.
# Two repos had them swept in by `git add -A` before this rule existed
# (dev-platform 4289d91: 3 files; EngramGraph 0a8e789: 360 files).
.uds-backup-*/
